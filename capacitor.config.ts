import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.ionic.weather.station',
  appName: 'app-ionic-weather-station',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
